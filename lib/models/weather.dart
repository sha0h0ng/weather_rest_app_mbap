class Weather {
  String weatherID;
  String weatherCondition;
  double weatherTemp;
  DateTime weatherDate;
  Weather(this.weatherID, this.weatherCondition, this.weatherTemp,
      this.weatherDate);
}
